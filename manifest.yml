---
applications:
  - name: integration-rabbitmq-template
    buildpack: java_buildpack
    path: target/rabbitmq-consumer-template-2.0.8.jar
    services:
        - myRabbitMQInstance
env:
    mainQueue: SampleMainQ
    sampleErrorQueue: SampleErrorQ
